import 'package:flutter/material.dart';

class AppRadius {
  static final xs = BorderRadius.circular(4.0);
  static final sm = BorderRadius.circular(8.0);
  static final md = BorderRadius.circular(16.0);
  static final lg = BorderRadius.circular(24.0);
  static final xl = BorderRadius.circular(32.0);
}
